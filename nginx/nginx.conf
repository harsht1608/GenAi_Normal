services:
  nginx:
    image: nginx:stable-alpine
    container_name: gemini-nginx
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend